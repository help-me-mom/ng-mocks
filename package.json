{
  "name": "karma-typescript-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for karma-typescript packages",
  "private": true,
  "author": "monounity",
  "contributors": [
    "erikbarke"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/monounity/karma-typescript.git"
  },
  "license": "MIT",
  "scripts": {
    "bootstrap": "npm install && lerna bootstrap --hoist --no-ci",
    "build": "lerna run build",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "ci": "npm run lint && npm run build && npm run test"
  },
  "dependencies": {
    "lerna": "^3.4.3"
  }
}
