/*
 * This comment should be left intact in the html coverage
 */
export class FormattingComponent {

    // Another comment, should not trip up the coverage remapping
    public oneliner(): string { let greeting = "This one line function kept it's formatting!"; return greeting; }
}
