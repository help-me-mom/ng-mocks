/*
 * This comment should be left intact in the html coverage
 */
export class FormattingTester {

    // Another comment, should not trip up the coverage remapping
    // tslint:disable-next-line:brace-style
    public testOneliner(): string { let greeting = "This one line function kept it's formatting!"; return greeting; }
}
