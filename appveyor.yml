environment:
  nodejs_version: "6"

install:
  - ps: Install-Product node $env:nodejs_version
  - ci\install.bat

test_script:
  - npm run lint
  - npm run build:ci
  - npm run test:unit
  - ci\run.bat

build: off
